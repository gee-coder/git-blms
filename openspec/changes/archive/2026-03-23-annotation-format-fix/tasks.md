## 1. 缓存失效修复

- [x] 1.1 在 `GitService` 中添加获取 Git HEAD commit hash 的方法
- [x] 1.2 修改 `BlameManager.createCacheKey()` 在缓存键中加入 Git HEAD
- [x] 1.3 添加 Git HEAD 结果的内存缓存（1秒过期）以优化性能
- [x] 1.4 处理 Git HEAD 获取失败的情况（降级处理）
- [x] 1.5 添加缓存失效的单元测试

## 2. 日期和时间格式化

- [x] 2.1 在 `relativeTime.ts` 中实现固定宽度日期格式 `yyyy/mm/dd`
- [x] 2.2 实现相对时间短格式：`{N}m ago`, `{N}h ago`, `{N}d ago`, `{N}w ago`
- [x] 2.3 实现固定10字符宽度，不足补空格的逻辑
- [x] 2.4 添加 `{N}y ago` 格式处理超过1年的提交
- [x] 2.5 更新中文相对时间格式的宽度计算
- [x] 2.6 添加日期格式化的单元测试

## 3. 用户名显示格式化

- [x] 3.1 在 `decoratorManager.ts` 中修改 `truncateAuthor()` 函数，限制6显示宽度
- [x] 3.2 实现用户名按显示宽度截断的逻辑（中文=2，英文=1）
- [x] 3.3 实现短用户名补空格至6显示宽度的逻辑
- [x] 3.4 使用单字符省略号 `…` 代替当前的多字符省略
- [x] 3.5 更新 `calculateAnnotationWidth()` 以适应新的宽度计算
- [x] 3.6 添加用户名格式化的单元测试

## 4. 国际化更新

- [x] 4.1 更新 i18n 翻译文件（相对时间已程序化生成，无需额外翻译）
- [x] 4.2 添加中文相对时间格式（已在程序中实现）
- [x] 4.3 验证各语言环境下的显示效果

## 5. 集成测试

- [x] 5.1 测试 git commit 后重新打开文件的缓存失效
- [x] 5.2 测试各种用户名长度的显示效果（中文、英文、混合）
- [x] 5.3 测试相对时间在不同时间点的格式化（分钟、小时、天、周、年）
- [x] 5.4 测试中英文环境下的标注显示宽度一致性
- [x] 5.5 测试文件保存、编辑、回车等操作的未提交行显示

## 6. 文档和清理

- [x] 6.1 更新 README 或 CHANGELOG 说明格式变化
- [x] 6.2 检查并更新相关的配置说明
- [x] 6.3 代码审查和清理
